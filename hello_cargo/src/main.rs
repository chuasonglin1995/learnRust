fn main() {
    println!("Hello, world!");
}

// cargo build
// ./target/debug/hello_cargo

// cargo run -> similar to above. build and run in one step

// cargo check -> checks code for errors, but doesn't build executable

// cargo build --release -> builds optimized version of executable